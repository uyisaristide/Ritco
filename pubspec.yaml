name: ritco_app
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: ^2.5.1
  go_router: ^14.1.1
  email_validator: ^2.1.17
  cached_network_image: ^3.3.1
  shared_preferences: ^2.2.3
  flutter_vector_icons: ^2.0.0
  multi_select_flutter: ^4.1.3
  url_launcher: ^6.3.0
  dio: ^5.5.0+1
  qr_flutter: ^4.1.0
  easy_localization: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true # Ensure this line is uncommented
  assets:
    - assets/images/
    - assets/translations/
